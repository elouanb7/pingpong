<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ path('home') }}">PingPong</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('home') }}">Home<span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('leaderboard') }}">Hall of Fame</a>
            </li>
            {% if app.user %}
            {% for role in app.user.roles %}
            {% if role == "ROLE_ADMIN"%}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('dashboard') }}">Dashboard</a>
                </li>
            {% endif %}
            {% endfor %}
            {% endif %}
            {#<li class="nav-item">
                <a class="nav-link" href="#">Features</a>
            </li>#}
        </ul>
        <span class="navbar-text">
            <ul class="navbar-nav mr-auto">
                {% if app.user %}
                    <li class="nav-item mr-2">
                    <a class="nav-link" href="{{ path('profile', {'id': app.user.id}) }}">Profil</a>
                </li>
                {% endif %}
                <li class="nav-item">
                    {% if app.user %}
                        <a class="btn btn-outline-primary loginbutton" data-toggle="modal" data-target="#logout"
                       role="button">Déconnexion</a>
                        {% else %}
                        <a href="{{ path('app_login') }}" class="btn btn-primary text-white ">Connexion</a>
                    {% endif %}
                </li>
            </ul>
    </span>
    </div>
</nav>